const chai = require('chai');
const expect = chai.expect;

describe('Default Test Placeholder', function() {
  it('Should always pass this test', function() {
    expect(1).to.equal(1);
  });

  it('Should always fail this test', function() {
    expect(1).to.equal(2);
  });
});